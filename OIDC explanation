in DDR differential data down class Interface, I see forget DDR account product list data. The return type is list map of string and object. But can you make can you able to make it like a collection of the DDR product list data data type so this is one change I could see. and



Instead of using @geetter @setter can you able to use @data in DDRAccountProductListDataItem



In DDRAccountProductListFlatFileHeaderCallback @override no usages

Implement below kind of implementation

public void writeHeader (Writer writer) throws IOException {

writer write(header);





Code changed: C

/**



﻿/

﻿﻿Format BigDecimal with 5 decimal precision, null-safe.

private String formatBigDecimal(BigDecimal value) & 81 usages ≤ vikramad

if (value != null) {

return value setScale( newScale: 5, RoundingMode.HALF_UP) toPlainString®;

}

return null;

/**



﻿/

﻿﻿Format date to string, null-safe.

private String formatDate(Object date) { return date != null ? String valueDf(date) : null; }

/**



﻿/

﻿﻿Remove conmas from string for CSV safety, trim spaces, null-safe.

private String formatsvSafeString(String value) & 82 usages ≤ vikramad

if (value != null) {

String trimmed = value.trimO;

// remove commas which break SV columns in Excel opening

return trimmed replace target: ",", replacement: "™);

return null;

public String[] getHeaderAsArray { return header.split(DELIMITER, limit: -1); }



Also impleemnt this in processor
